# AGENTS.md(AI 编码代理工作约定,每次会话先读完本文件再动手)

## 会话开始:先恢复项目状态

1. **必读 `docs/项目状态_交接.md`**——它是本项目的唯一可信状态源(项目一句话状态、待办队列、决策记录、踩坑记录),读完再动手;配套必读 `README.md`。
2. 不要凭印象或上一次对话的残留记忆接续工作:一切以交接文档和 `git log` 为准;文档没写的状态就是不存在。

## 交接文档实时维护

3. **实时更新,不许攒到最后**:完成一个任务、用户拍板一个决策、踩到一个坑、状态发生任何变化,当场写入交接文档对应章节(§0 一句话状态 / §3 待办队列 / §5 踩坑记录 / §6 决策记录),再继续下一步。
4. 会话收尾前自查:本次会话的产出、未决事项、下一步入口是否都已落进交接文档;没有就补完再结束。

## git 提交与推送流程

5. **推送默认走 PR 流程**:切功能分支 → push 分支 → 开 PR → 用户确认后 merge;**不直推 main**。
6. 仅当用户明确说"直推"(或等效表述)时,才允许直接 `push origin main`。
7. 用户说"不要推送,等确认"期间,commit 只落本地,推送动作等用户指令。
8. commit message 沿用仓库中文详述风格:一条 commit 讲清做了什么、为什么、怎么验证的;多任务大批次按任务分节;每项改动以实测结果为依据,没实测的写"未实测"。
